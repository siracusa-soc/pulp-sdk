APP = main
APP_SRCS += main.c
APP_CFLAGS += -O3 -g 


APP_CFLAGS += -DEXEC_LATENCY=$(ET)

APP_CFLAGS += -DN_ADCS=$(AD)
APP_CFLAGS += -DSMEM_ADDR=$(B)

APP_CFLAGS += -DADC_D0_LENGTH=$(A0L)
APP_CFLAGS += -DADC_D1_LENGTH=$(A1L)
APP_CFLAGS += -DADC_D2_LENGTH=$(A2L)
APP_CFLAGS += -DROW_D0_LENGTH=$(R0L)
APP_CFLAGS += -DROW_D1_LENGTH=$(R1L)
APP_CFLAGS += -DROW_D2_LENGTH=$(R2L)
APP_CFLAGS += -DFRAME_D0_LENGTH=$(F0L) 
APP_CFLAGS += -DFRAME_D1_LENGTH=$(F1L)
APP_CFLAGS += -DFRAME_D2_LENGTH=$(F2L)
APP_CFLAGS += -DADC_D0_STRIDE=$(A0S)
APP_CFLAGS += -DADC_D1_STRIDE=$(A1S)
APP_CFLAGS += -DADC_D2_STRIDE=$(A2S)
APP_CFLAGS += -DROW_D0_STRIDE=$(R0S)
APP_CFLAGS += -DROW_D1_STRIDE=$(R1S)
APP_CFLAGS += -DROW_D2_STRIDE=$(R2S)
APP_CFLAGS += -DFRAME_D0_STRIDE=$(F0S)
APP_CFLAGS += -DFRAME_D1_STRIDE=$(F1S)
APP_CFLAGS += -DFRAME_D2_STRIDE=$(F2S)


PMSIS_OS=pulpos

include $(RULES_DIR)/pmsis_rules.mk