APP = main
APP_SRCS += main.c
APP_CFLAGS += -O3 -g 

APP_CFLAGS += -DH_RESOLUTION=$(HR)
APP_CFLAGS += -DV_RESOLUTION=$(VR)
APP_CFLAGS += -DN_FRAMES=$(F)
APP_CFLAGS += -DSENSOR_TYPE=$(S)
APP_CFLAGS += -DEXPOSURE_LATENCY=$(ET)
APP_CFLAGS += -DRESET_LATENCY=$(RT)
APP_CFLAGS += -DREADOUT_LATENCY=$(AT)
APP_CFLAGS += -DN_ADCS=$(AD)
APP_CFLAGS += -DBUFFER1_ADDR=$(B1)
APP_CFLAGS += -DBUFFER2_ADDR=$(B2)
PMSIS_OS=pulpos

include $(RULES_DIR)/pmsis_rules.mk