APP = main
APP_SRCS += main.c
APP_CFLAGS += -O3 -g -Iinc
APP_CFLAGS += -DEXPOSURE_LATENCY=$(EL)
APP_CFLAGS += -DRESET_LATENCY=$(RL)
APP_CFLAGS += -DREADOUT_LATENCY=$(DL)
APP_CFLAGS += -DCFG_N_ITERS=$(NI)
APP_CFLAGS += -DCFG_N_ROWS=$(NR)
APP_CFLAGS += -DCFG_N_COLS=$(NC)
PMSIS_OS=pulpos

include $(RULES_DIR)/pmsis_rules.mk